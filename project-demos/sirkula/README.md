# Sirkulær

A simple static web project featuring an interactive HTML/CSS design.

## 🔍 Overview

This repository contains the source files for a website named **Sirkulær** (`index.html` + `style.css`). The project is intended to present content or visuals in a circular layout style (based on the project name), and is built using standard **HTML and CSS**.

## 🧠 What’s Inside

- `index.html` — The main page of the project, built with semantic HTML.
- `style.css` — Stylesheet that defines the visual design of the site.
- (Optional) Future assets — such as images, scripts, or additional pages.

## 🚀 How to Use

You can view your project locally in 3 easy steps:

1. **Download or clone** the repo:
   ```bash
   git clone https://github.com/ivantodosiks-png/Sirkul-r.git

---